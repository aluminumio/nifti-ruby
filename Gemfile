source "https://rubygems.org"

gemspec

# Optional at runtime, required by the writer spec to construct typed arrays
# matching nibabel's numpy fixtures. Keeping it out of the gemspec so the gem
# stays dependency-free for read-only consumers.
gem "numo-narray", "~> 0.9"
